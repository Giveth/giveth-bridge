const { GivethBridge, CSTokenMinter, ForeignGivethBridge, CSTokenRegistry } = require('./lib/contracts');

module.exports = {
    GivethBridge,
    ForeignGivethBridge,
    CSTokenMinter,
    CSTokenRegistry
};
